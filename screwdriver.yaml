version: 4
   
shared:
  image: node:12
    
annotations:
  screwdriver.cd/ram: 5

jobs:
  pull-request:
    requires: [~pr]
    steps:
      - build: echo "skip build"
  component:
    steps:
      - build: echo "build"
      - lscpu: lscpu
      - meminfo: cat /proc/meminfo
  component1:
    steps:
      - build: echo "build"
  component2:
    steps:
      - build: echo "build"
  component3:
    steps:
      - build: echo "build"
   
